<h1>Listing current Trello cards:</h1>

<br>
<div class="panel-default">
  <table class="table-striped custom-stripe table-bordered custom-border sortable" width="100%" align="center">
    <tr>
      <th width="20%">Card Name</th>
      <th width="20%">Current Lane</th>
      <th width="20%">Date Placed in Resumes Lane</th>
      <th width="20%">Date Placed in End Lane ("Success - " or any "Unsuccessful - ")</th>
      <th width="20%">Duration of Card, <font color="#ff0000">* means the card is not yet complete</font></th>
    </tr>
    <% if @cards.present? %>
        <% @cards.each do |card| %>
        <tr>
          <td><%= card.name %></td>
          <td><%= card.list_name %></td>
          <td><%= CardsHelper.show_start_date(card) %></td>
          <td><%= CardsHelper.show_end_date(card) %></td>
          <td><% if CardsHelper.show_difference_in_time(card).include?('*') %>
                <font color="#ff0000"> <%= CardsHelper.show_difference_in_time(card) %> </font>
            <% else %>
                <%= CardsHelper.show_difference_in_time(card) %>
          <% end %></td>
        </tr>
        <% end %>
      <% else %> <p> No cards. </p>
    <% end %>
  </table>
  <br>
</div>
